# 环境测试服务器

## 说明

- 本文件记录当前项目可用的真实测试环境入口、域名与相关运维资源。
- 仅作为本地执行与联调用途，不应复制到公开渠道。
- 后续若服务器用途重新分配，需要在本文件持续更新。

## 测试目标

- 禁止以 mock 数据作为最终验收依据。
- 允许直接在本项目仓库中修改代码，并在真实服务器上完成部署与验证。
- 最终目标是让项目达到可进行真实环境测试的完整状态。

## 服务器列表

### 服务器一

- IP: `38.58.59.119`
- 登录方式: `root` 密码登录
- 密码: `Cnyirui88635540`
- 备注: 钱包服务器

### 服务器二

- IP: `38.58.59.142`
- 登录方式: `root` 密码登录
- 密码: `ypbaHXDG9534`
- 备注: 钱包服务器+数据库服务器+vpn服务器

### 服务器三

- IP: `154.37.208.72`
- 登录方式: `root` 密钥登录
- 密钥:  `/users/cnyirui/server/154.37.208.72/keys/154.37.208.72.pem`
- 备注: 当前首轮真实联调主机

## 域名与证书

- 主域名: `residential-agent.com`
- API 子域: `api.residential-agent.com`
- 旧联调子域: `vpn.residential-agent.com`
- 预留钱包子域:
  - `sol.residential-agent.com`
  - `usdt.residential-agent.com`
  - `wallet.residential-agent.com`
- 证书目录: `/users/cnyirui/server/ssl/residential-agent.com`

## Cloudflare

- Cloudflare API 目录: `/users/cnyirui/server/cloudflare`

## 当前执行约束

- 事实源优先级:
  1. `final_engineering_delivery_package`
  2. 原始需求文档
  3. 当前代码现状
  4. 历史 handoff / 旧总结文档
- 对 `final_engineering_delivery_package` 的处理原则:
  - 尽量保留
  - 仅做最小修补
  - 目标是减少整体工作量
- 执行模式:
  - 我方主控
  - 本地 Kimi 负责主要执行
  - 主控负责拆解、审核、回退、放行与验收
- 工作链路:
  - 规格收敛
  - `bd` 任务树重建
  - 指挥 Kimi 分批开发
  - 审核回退 / 放行
  - 本地与真实环境关键链路验证

## 当前主控决定

- API 接口统一使用 `api` 子域。
- 钱包/链侧后续按能力拆到独立子域:
  - `sol`
  - `usdt`
  - `wallet`
- 当前 `vpn.residential-agent.com` 仍可保留为兼容入口，但不再作为长期唯一 API 域名。

## 当前实际状态

- 当前真实联调仍是**临时集中式**，不是最终三机拆分态。
- 服务器三 `154.37.208.72`
  - 正在承载新 backend 运行与 API 网关
  - 已接入 `api.residential-agent.com`
- 数据库、Redis、钱包链侧服务还没有完全拆出独立正式角色
  - 这一点已单独建成任务 `liaojiang-2f0.1`

## 目标分工

- 服务器一 `38.58.59.119`
  - `sol.residential-agent.com`
  - `usdt.residential-agent.com`
  - Solana 钱包 / 链侧服务
  - TRON / TRC20-USDT 钱包 / 链侧服务
  - 最小职责:
    - SOL / SPL-USDT 广播兜底
    - Solana 链侧支付扫描
    - 地址 / 余额 / 交易状态查询辅助
    - TRON / TRC20 广播兜底
    - TRC20 链侧支付扫描
    - txHash 详情查询
  - 不纳入首批:
    - 自建 Solana 全节点
    - 自动扫资归集
    - Swap / DEX / 代签名
    - 多链聚合
    - 自动提现
- 服务器二 `38.58.59.142`
  - PostgreSQL / Redis
  - 不承载对外钱包链侧 HTTP 服务
- 服务器三 `154.37.208.72`
  - `api.residential-agent.com`
  - Nginx / backend / 外部 API 接入层
  - 必要时承载 admin 接入

## API 与钱包服务边界

- `api.residential-agent.com`
  - 账号、订单、订阅、邀请、佣金、提现审核、状态机推进
  - 不做链侧重逻辑
- `sol.residential-agent.com`
  - 只负责 Solana 链侧能力
  - 通过内部 HTTP / 队列与 API 交互
- `usdt.residential-agent.com`
  - 只负责 TRON / TRC20-USDT 链侧能力
  - 通用 USDT 账本与结算仍留在 API 层
- `wallet.residential-agent.com`
  - 预留
  - 暂不在首批落地，避免过早做“通用钱包中台”

## 当前真实环境进度

### 已确认

- `154.37.208.72`
  - 可 SSH 连接
  - 已安装 `docker`
  - 已安装 `psql`
  - 已安装 `node`
  - 已安装 `npm`
- `38.58.59.119`
  - 可 SSH 连接
  - 当前未确认 `docker` / `psql`
- `38.58.59.142`
  - 当前存在 host key 变更告警，暂未继续密码直连

### 服务器三当前状态

- 代码已同步到:
  - `/opt/cryptovpn/backend`
  - `/opt/cryptovpn/deploy`
- 宿主机方式启动 backend 成功
- Nginx 已新增 `vpn-residential-agent.conf`
  - `api.residential-agent.com` 为唯一 API 主入口
  - `vpn.residential-agent.com` 降级为兼容入口，不再作为主 API 域名
  - `/api/* -> 127.0.0.1:3000`
- Cloudflare 已完成:
  - 删除 `vpn.residential-agent.com -> 38.58.59.142` 错误 A 记录
  - 新增 `api.residential-agent.com -> 154.37.208.72`
- 已验证公网接口:
  - `GET https://api.residential-agent.com/api/healthz`
  - `POST https://api.residential-agent.com/api/client/v1/auth/register/email/request-code`
- 已验证业务 smoke:
  - request-code
  - register
  - me
  - plans
  - orders
  - payment-target
  - referral overview
  - commissions summary
  - withdrawals 预期返回余额不足
- 已复用现有 PostgreSQL 容器 `vpn-panel-db`
  - 端口: `127.0.0.1:25432`
  - 已创建独立测试库: `cryptovpn_test`
  - 已执行 baseline SQL
  - 已执行 bootstrap seed

### 服务器二当前状态

- 已新建 `/opt/cryptovpn-data`
- 已运行容器:
  - `cryptovpn-data-postgres-1`
  - `cryptovpn-data-redis-1`
- 当前作为 DB / Redis 物理落点
- 当前不作为对外钱包链侧服务落点
- 由于服务器三无法直接连入服务器二的 `22/5432/6379`，当前采用:
  - 服务器二 -> 服务器三 的反向 SSH 隧道
  - 在服务器三暴露本地:
    - `127.0.0.1:15432`
    - `127.0.0.1:16379`
- 服务器三 backend 当前通过隧道本地端口访问服务器二状态服务

### 当前限制

- `docker-compose.backend.yml` 在该服务器上构建 backend 镜像时，容器内访问 npm registry 出现 `EAI_AGAIN`，当前未完全解决
- 因此 backend 当前采用 **宿主机 Node 进程 + 现有 PostgreSQL 容器** 的临时联调方案
- 这套方案可用于当前真实环境测试，但还不是最终发布形态
- 三机角色拆分与 `sol/usdt/wallet` 子域还未正式落地
