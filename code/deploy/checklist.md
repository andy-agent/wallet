# v2rayng-payment-bridge 上线检查表

## 检查清单使用说明
- [ ] 未检查项
- [x] 已检查项
- 每一项都必须确认后才能上线

---

## 1. 环境准备

### 1.1 服务器环境
- [x] 服务器系统：Ubuntu 24.04 LTS
- [x] Docker 版本 >= 24.0
- [x] Docker Compose 版本 >= 2.0
- [ ] 服务器时区设置为 UTC 或本地时区
- [ ] NTP 时间同步已启用
- [x] 防火墙已配置（只开放必要端口 8080/8443）

### 1.2 域名和证书
- [ ] 域名已解析到服务器
- [ ] SSL 证书已准备（Let's Encrypt 或商业证书）
- [ ] HTTPS 强制跳转已配置

---

## 2. 安全配置

### 2.1 密钥生成
```bash
# 生成 JWT Secret（32字节）
openssl rand -hex 32

# 生成加密主密钥（32字节 base64）
openssl rand -base64 32

# 生成 Admin Token
openssl rand -hex 16
```

- [ ] JWT_SECRET 已生成且长度 >= 32 字符
- [ ] ENCRYPTION_MASTER_KEY 已生成（base64 格式）
- [ ] ADMIN_TOKEN 已生成且强度足够
- [ ] 以上密钥已安全保存（密码管理器或保险箱）

### 2.2 数据库安全
- [ ] 数据库密码强度足够（>= 16 位，含大小写+数字+符号）
- [ ] 数据库未暴露到公网
- [ ] 数据库定期备份已配置

### 2.3 Marzban 集成
- [ ] Marzban 服务可访问
- [ ] Marzban 管理员账号密码正确
- [ ] Marzban API 测试通过

---

## 3. 区块链配置

### 3.1 Solana
- [ ] RPC 节点可用（主网或 QuickNode）
- [ ] 地址池已导入（至少 100 个地址）
- [ ] 私钥已加密存储

### 3.2 Tron
- [ ] RPC 节点可用（TronGrid 或自建）
- [ ] USDT 合约地址正确
- [ ] 地址池已导入（至少 100 个地址）
- [ ] 私钥已加密存储

---

## 4. 部署前测试

### 4.1 单元测试
```bash
cd code/server
pytest tests/ -v
```
- [ ] 所有单元测试通过

### 4.2 集成测试
- [ ] 创建订单流程测试通过
- [ ] 支付检测流程测试通过（使用 Mock 模式）
- [ ] 开通账号流程测试通过
- [ ] 订阅拉取测试通过

### 4.3 端到端测试（测试网）
- [ ] Solana Devnet 支付全流程测试
- [ ] Tron Nile 支付全流程测试
- [ ] 续费流程测试
- [ ] 异常订单处理测试

---

## 5. 部署配置

### 5.1 环境变量
- [x] `.env` 文件已创建 (code/deploy/.env)
- [x] 所有必需变量已填充
- [ ] 无默认值保留在生产环境
- [ ] `.env` 文件权限设置为 600

### 5.2 Docker Compose
- [x] `docker-compose.test.yml` 已检查
- [x] 镜像版本已固定（使用 alpine 版本）
- [x] 重启策略已配置 (unless-stopped)
- [x] 健康检查已配置

### 5.3 Nginx 配置
- [ ] `nginx.conf` 已配置
- [ ] SSL 证书路径正确
- [ ] 反向代理配置正确
- [ ] 限流已配置（防止 DDoS）

---

## 6. 上线部署

### 6.1 数据库 (测试服务器)
```bash
cd /opt/payment-bridge/deploy
docker-compose -f docker-compose.test.yml up -d postgres redis
```
- [x] PostgreSQL 配置完成 (端口不暴露)
- [x] Redis 配置完成 (端口不暴露)
- [ ] 数据库迁移执行成功：`alembic upgrade head`
- [x] SSL 配置完成

### 6.2 应用服务 (测试服务器)
```bash
cd /opt/payment-bridge/deploy
docker-compose -f docker-compose.test.yml up -d api worker wallet
```
- [x] API 服务配置完成 (端口 8080)
- [x] Worker 服务配置完成
- [x] Wallet 服务配置完成 (仅内部网络)
- [x] Nginx 配置完成 (端口 8443，可选)

### 6.3 健康检查 (测试服务器)
```bash
# API 健康检查
curl http://154.36.173.184:8080/healthz

# 套餐列表检查
curl http://154.36.173.184:8080/client/v1/plans
```
- [ ] 健康检查接口返回 200
- [ ] 套餐列表接口返回正常数据

---

## 7. 数据初始化

### 7.1 套餐导入
- [ ] 默认套餐已创建（月度/年度）
- [ ] 套餐价格正确
- [ ] 套餐支持的支付方式正确

### 7.2 地址池导入
```bash
# 导入 Solana 地址
POST /admin/v1/address-pool/import

# 导入 Tron 地址
POST /admin/v1/address-pool/import
```
- [ ] Solana 地址池已导入（>= 100 个）
- [ ] Tron 地址池已导入（>= 100 个）
- [ ] 地址池状态检查正常

---

## 8. 监控和日志

### 8.1 日志配置
- [ ] 日志收集已配置（可选：ELK / Loki）
- [ ] 日志保留策略已设置
- [ ] 错误日志告警已配置

### 8.2 监控指标
- [ ] API 响应时间监控
- [ ] 订单状态监控
- [ ] 地址池余量监控
- [ ] 链上监听状态监控

---

## 9. 客户端配置

### 9.1 Android 客户端
- [ ] API 基础 URL 配置正确
- [ ] 版本号更新
- [ ] 测试环境切换为生产环境

### 9.2 应用商店
- [ ] 应用截图已更新
- [ ] 应用描述已更新（含支付说明）
- [ ] 隐私政策已更新

---

## 10. 应急预案

### 10.1 回滚方案
- [ ] 上一版本镜像已备份
- [ ] 数据库备份已创建
- [ ] 回滚命令已准备

### 10.2 紧急联系人
- [ ] 技术负责人联系方式
- [ ] Marzban 管理员联系方式
- [ ] 服务器提供商联系方式

---

## 11. 最终确认

- [ ] 所有检查项已完成
- [ ] 测试订单已创建并通过
- [ ] 监控仪表盘已确认正常
- [ ] 团队已准备好处理线上问题

---

## 上线后检查（部署后 1 小时内）

- [ ] 服务正常运行，无异常重启
- [ ] 订单创建正常
- [ ] 支付检测正常
- [ ] 账号开通正常
- [ ] 无异常错误日志
- [ ] 监控指标正常

---

**上线日期**: _______________
**执行人**: _______________
**确认人**: _______________
